# 2.导航

## 相对路径和绝对路径

**绝对路径：**以根目录作为起点，以/开头

**相对路径：**以当前目录为起点

**特殊路径符**：

```c++
.表示当前目录 ./Desktop 和Desktop意思是一样的
..表示上一级目录
~表示用户的home目录 等价/home/creep/
```



## cd

**切换当前工作目录，无选项**

**参数**

路径



# 3.探索linux系统

## ls

**查看目标目录的内容**

**选项**

- -a 显示隐藏的内容（以.开头的文件）
- -A 与-a相同 不列出当前目录(.)和父目录(..)
- -l以列表的形式显示内容
- -h 要和-l配合使用，更合适的显示文件的大小单位
- -d 查看目录本身而不是目录里的内容
- -F 每个列出的名称后面加类型指示符，例如如果是目录名则后面会加/
- -r 以降序排列
- -S 按文件大小排序
- -t按修改时间排序

**参数**

路径



## file

确定文件类型

```c++
file filename
```



## less

查看文本文件

```c++
less filename
```

**命令**

- pageup/b 上翻页
- pagedown/空格 下翻页
- G 移动到文件末尾
- 1G/g 移动到文件开头
- /characters 向前搜索字符串
- n 重复上一次搜索
- h 显示帮助信息
- q退出



# 4 操作文件和目录

## 通配符及其含义

- `*` 匹配任意多个字符
- `?` 匹配任意单个字符
- `[characters]`匹配属于字符集的
- `[!characters]`匹配不属于字符集的
- `[[:class:]]` 匹配属于字符类class的任意单个字符



**常见的字符类**

- `[:alnum:]` 匹配任意单个字符字母数字
- `[:alpha:]` 匹配任意单个字母
- `[:digit:]` 匹配任意单个数字
- `[:lower:]` 匹配任意单个小写字母
- `[:upper:]` 匹配任意单个大写字母



e.g

```c++
b*.txt 以b开头拓展名为.txt的文件
[a,b,c]* 以a,b,c中任意字符开头的文件
[![:digit:]]* 不以数字开头的文件
```



## pwd

**打印当前的工作目录，没有选项没有参数**





## **mkdir **

**创建新的目录**

**参数**

要创建目录的路径

**选项**

-p 可用于连续创建多级目录





## touch

**创建文件，无选项**

**参数**

路径





## cat

**查看文件内容**

**参数**

要查看的文件路径（要查看的内容不能是文件夹）





## more

**查看文件内容**

**参数**

要查看的文件内容

空格进行翻页，q退出查看



## cp

**复制文件和文件夹**

**参数**

参数1 表示被复制的路径 

参数2 表示复制到的路径

**选项**

- -r 用于文件夹的复制
- -a 复制文件目录及其包括的所有权和权限
- -i 交互
- -u 只复制参数1中参数2里没有的文件
- -v显示相关信息







## mv

**移动和重命名文件和文件夹**

**参数**

参数1 表示被移动的路径 

参数2 表示移动到的路径

**选项**

- -i 交互
- -u  只移动参数1中参数2里没有的文件
- -v 显示相关信息





## **rm**

**删除文件和文件夹**

**参数**

可以有无穷个，表示要删除的文件和文件夹

**选项**

- -r 删除文件夹
- -f 强制删除(root用户删除的时候如果不加-f删除的时候要询问你是否删除)
- -i 交互
- -v显示相关信息



## ln

**创建硬链接和符号链接（软链接）**

**硬链接**

```c++
ln file link
```

- 文件和连接必须在用以磁盘分区
- 硬链接不能引用目录

**符号链接**

```c++
ln -s item link
```

- 相当于快捷方式



# 5.和命令打交道

## 识别命令

### type

显示命令是哪种形式

```c++
$ type ls
ls is aliased to `ls --color=auto'
```



### which

显示可执行文件的位置

```c++
which 可执行文件
```



## 获取命令文档

### help

**获取shell内置命令的帮助信息**

```c++
help cd
```



### man

**显示命令的手册页**

```c++
man ls
```



## alias

**创建自己的命令**

```c++
alias 别名='命令1；命令2；....'
e.g alias foo ='cd /usr;ls ;cd -'
```



直接输入alias可以看所有的别名

### unlias

 **删除别名**

```c++
ulias 别名
```



# 12.vim入门

## 三种工作模式

- 命令模式

vim/vi 文件路径

如果文件路径表示的文件不存在，那么会创一个新文件

- 插入模式
- 底线命令模式



<img src="C:\Users\LENOVO\AppData\Roaming\Typora\typora-user-images\image-20260119232513167.png" alt="image-20260119232513167" style="zoom:50%;" />

## 启动和退出vim

启动：

```c++
vim 文件路径
```

如果文件路径不存在会自动创建



退出：

- `q`退出
- `q!`强制退出不保存
- `w`保存



## 底线命令模式

- `:wq`保存并退出
- `:q`仅退出
- `:q!`强制退出
- `:w`仅保存
- `:set nu`显示行号
- `:set paste` 设置粘贴模式



## 光标移动

- `l/右方向键` 向右移动一个字符
- `h/左方向键` 向左移动一个字符
- `k/上方向键` 向上移动一个字符
- `j/下方向键` 向下移动一个字符
- `w` 移动到下一个单词开头/标点符号
- `W` (大写)  移动到下一个单词开头，忽略标点符号
- `b` 移动到上一个单词开头/标点符号
- `B`  移动到上一个单词开头，忽略标点符号
  - `ctrl+f/下翻页键` 移动到下一页
- `ctrl+b/上翻页键` 移动到上一页
- `数字+G` 移动到第数字行
- G 移动到最后一行



## 基本编辑

### 追加

**进入插入模式**

- `i` 在光标位置进入，由于在命令模式光标不能移到行尾，所以行尾进入插入模式只能使用a/A

- `a` 在光标的下一个位置进入

- `A` 光标移到行尾进入



### 新建

**新建行后会进入插入模式**

- `o` 在当前行下一行新建一行
- `O` （大写） 在当前行上一行新建一行



### 删除

**删除命令**

- `x`当前字符
- `3x`当前字符和后面两个字符
- `dd`当前行
- `5dd`当前行和接下来4行
- `dW`从光标所在处到下一个单词开头
- `d$`从光标所在处到行尾
- `d0`从光标所在处到行首
- `d^`从光标所在处到第一个非空白字符
- `dG`从当前行到文件末尾
- `d20G`从当前行到第二十行



### 剪切，复制，粘贴

**d命令删除的内容本质上放到了剪切板，可以粘贴**

- `p` 粘贴

- `u`撤销

**复制命令**

- `yy`当前行

- `5yy`当前行和接下来四行

- `yW`从光标所在处到下一个单词开头
- `y$`从光标所在处到行尾
- `y0`从光标所在处到行首
- `y^`从光标所在处到第一个非空白字符
- `yG`从当前行到文件末尾
- `y20G`从当前行到第二十行



### 合并

`J` (大写) 删除光标所在行的换行符



## 搜索和替换

### 搜索整个文件

- `/`进入搜索模式
- `n`继续向下搜素
- `N`继续向上搜素



### 全局搜索和替换

e.g 要将所有的Line换成line

```c++
:%s/Line/line/g
```

- : 冒号接下来是ex命令
- % 指定操作的行范围，%是简便写法表示第一行到最后一行，1,5表示第一行到第五行，1,$表示第一行到最后一行，未指定行范围默认本行
- s 指定操作 s->substitution(替换)
- g 代表global 对行中所有字符串进行替换操作，如果不指定只替换每行第一个搜索到的

可在最后加上c，变成 :%s/Line/line/gc，那么每次替换都会询问用户

```c++
replace with Line (y/n/a/q/l/^E/^Y)?    
```

- y 执行替换
- n跳过这次替换
- a执行所有替换
- q/esc 退出替换
- l 执行本次替换并退出
- ctrl+e/ctrl+y 向下滚动和向上滚动



## 编辑多个文件

vim加多个文件名



### 在文件之间切换

- :bn 切换到下一个文件
- :bp 切换到上一个文件

:**buffers 可以查看被编辑的文件**

```c++
:buffers
  1 %a   "1.txt"                        line 11
  2 #    "2.txt"                        line 2
```

第一列表示缓冲区号

可以通过 **:buffer 缓冲区号   来切换文件**



### 载入更多的文件进行编辑

- :e 文件名 



### 将一个文件内容复制到另一个文件

在一个文件内yy后 通过buffer切换缓冲区然后p

**如果要将整个文件a复制到另外一个文件b**

打开文件b

然后

```c++
:r a
```





# 13.定制提示符

##  分解提示符

```c++
creep@LAPTOP-395G2AEK:~$
```

包含 用户 主机名 工作目录

**提示符由环境变量PS1定义的**

```c++
creep@LAPTOP-395G2AEK:~$ echo $PS1
\[\e]0;\u@\h: \w\a\]${debian_chroot:+($debian_chroot)}\[\033[01;32m\]\u@\h\[\033[00m\]:\[\033[01;34m\]\w\[\033[00m\]\$
```



提示符中用到的转义字符

- `\a` 蜂鸣声
- `\d` 当前日期 “星期几 月 日” Mon May 26
- `\h` 本地服务器的主机名，不包含结尾域名
- `\H` 完整的主机名
- `\j` 执行在当前shell的作业数量
- `\l` 当前终端设备名称
- `\n` 换行符
- `\r` 回车符
- `\s` shell程序的名称
- `\t` 24小时制当前时间（时：分：秒）
- `\T` 12小时制当前时间
- `\@` 12小时制当前时间（AM/PM)
- `\A` 24小时制当前时间(时：分)
- `\u` 当前用户名
- `\v` shell版本号
- `\V` shell版本号和发布号
- `\w` 当前工作目录
- `\W` 当前工作目录的最后一部分
- `\!` 当前命令的历史记录编号
- `\#`当前shell会话中输入过的命令数量
- `\$` 显示$ 超级用户显示`#`
- `\[`  非输出控制字符开始 比如颜色或改变光标位置
- `]\`非输出控制字符结束



## 增加颜色

用于设置文本颜色的转义代码

```c++
\033[0;30m 黑色 \033[0;31m 红色 \033[0;33m 绿色 等
```

变色结束处要加 

```c++
\[\033[0m\]
```

不然提示符后终端输入的文本也变色了



用于设置文本背景色的代码

```c++
\033[0;40m 黑色 \033[0;41m 红色
```



## 移动光标

用于移动光标的转义代码

- \033[1;cH  将光标移到第一行c列
- \033[nA 将光标向上移动n行



## 保存提示符

将

```c++
PS1=...
export PS1
```

加入到~/.bashrc中



# 14 软件包管理

- 低层工具：处理软件包的安装和删除
- 高级工具：执行原数据搜索和解决依赖性问题

| linux发行版 | 低层工具 | 高层工具 |
| ----------- | -------- | -------- |
| Debian类    | dpkg     | apt      |
| Red Hat类   | RPM      | YUM      |



## 常见的软件包操作

### 查找安装删除软件包

#### 在仓库中操作

```c++
apt/yum search/install/remove package_name
```

**apt 进行操作之前要先 apt update**



#### 通过软件包文件

安装

```c++
dpkg/rpm -i package_file
```

更新

```c++
dpkg -i package_file
rpm -U package_file
```



### 列举已安装的软件包

```c++
dpkg -l 
rpm -qa
```



### 确定软件包是否安装

```c++
dpkg -s package_name
rpm -q package_name
```



### 显示已安装软件包的相关信息

```c++
apt show package_name
yum info package_name
```



### 识别某个文件是哪个软件包安装的

```c++
dpkg -S file_name
rpm -qf file_name
```





# 15 存储介质

## 存储设备的挂载与删除

**挂载：一个文件系统比如u盘接入linux总目录树里，你在linux里创建一个文件夹然后挂载到文件系统，那么访问那个文件夹就是访问那个文件系统**

### **查看已挂载的文件系统列表**

```c++
mount 
```

列表格式为：挂载点 type 文件系统类型 (挂载形式)

### 挂载到新的挂载点

```c++
mkdir /mnt/cdrom #先创建新的文件夹
mount /dev/sdc旧 /mnt/cdrom新
可加[-t iso9660（文件类型）] 指定文件类型
```

### 删除挂载点

```c++
umount /dev/hdc 旧的挂载点
```



# 16 联网

## 网络检查与监控

### ping

检查网络服务器是否联通

```c++
ping [-c num] ip/主机名
```

- -c 检查的次数 num 如果不使用-c 将无数次持续检查
- 参数 被检查的ip或主机名地址



### traceroute

列出网络流量从本机系统到指定主机经过的跳数（hop）

```c++
traceroute ip
```



### ip

检查系统的网络接口和路由表

```c++
ip a
```

有两个网络接口

- 环回接口lo
- 以太网接口eh0 

**网络诊断**：第一行中寻找UP字样和第三行inet字段是否存在有效的ip



### netstat

```c++
netstat -ie
```

检查系统的网络接口



```c++
netstat -r
```

显示内核的网络路由表



```c++
netstat - anp |grep 端口号
```

查看指定端口的占用情况



# 17 查找文件

## locate-简单的文件查找方法

从一个数据库里对路径名进行快速的查找

```c++
locate 要查找的文件名
```



## find-复杂的文件查找方法

### 测试条件

#### -type 文件类型

按类型查找

常见文件类型

- b 块设备文件
- c 字符设备文件
- d 目录
- f 普通文件
- l 符号链接

#### -name 

按名字查找，支持通配符

#### -size

按文件大小查找

```c++
find 起始路径 -size +|-n[kMG]
```

`+`,`-`表示大于小于

n表示大小数字

kMG表示大小单位（k小写其他两个大写）k表示kb，M表示NB，G表示GB,还有b表示512B，c表示B，w表示2B

- 查找<10kb的文件

  ```c++
  find / -size -10k
  ```

- 查找>100MB的文件

  ```c++
  find / -size +100M
  ```

e.g 

```c++
find ~ -type f -name "*.jpg" -size +1M |wc -l
```

#### -perm

```c++
-perm mode
```

匹配指定权限为mode的文件或目录，mode可以用八进制表示法或符号表示法

#### -newer

匹配内容发生变化时间比file更晚的文件或目录

### 操作符

- -and 与 

- -or 或

- -not 非

- 要使用括号的话要加转义字符 

  ```c++
  \( \)
  ```

  e.g

  ```c++
  find ~ \( -type f -not -perm 0600 \) -or \( -type d -not -perm 0700 \)
  ```

  



### 预定义操作

对结果执行某些操作

- -delete 删除当前匹配的操作
- -ls 将查找到的文件进行ls
- -print 将匹配文件的完整路径名输出至标准输出，如果没有指定其他的操作这是默认操作
- -quit 一旦发生匹配就退出

e.g

```c++
find ~ -type f -name "*.bak" -delete #删除后缀为.bak的文件
```

**操作符之间的关系默认是-and**

也就是上面其实等效于

```c++
find ~ -type f and -name "*.bak" and -delete
```



### 用户自定义操作

用法为

```c++
-exec command '{}' ';'#由于shell的{} ;有特殊用途所以要加标注
```

也可以用-ok换-exec，每次执行命令的时候都会提醒用户

e.g

```c++
find ~ -type f -exec ls -l '{}' ';'
```



#### 提高效率

之前的查找类似于

```c++
ls -l file1
ls -l file2
```

可以将查找结果合并起来执行

```c++
ls -l file1 file2
```

**方法：将原本句尾的';'改为+**



### xargs

从标准输入接收输入，将其转化为指定命令的参数列表

如上面那个例子可以改写为

```c++
find ~ -type f |xargs ls -l
```



### find命令选项

- -depth 要求find命令处理文件夹时先处理文件夹里的文件，指定-delete操作则自动应用此选项

- -maxdepth levels 设置find命令在执行操作时向下深入的最大目录层级数

- -mindepth levels 设置find命令在执行操作时向下深入的最小目录层级数

- -mount 不便利挂载到其他文件系统的文件夹

  

# 18 归档与备份



## 压缩文件


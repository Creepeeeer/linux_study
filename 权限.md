#### root用户

root用户拥有最大的系统操作权限，普通用户在许多地方操作是受限的

普通用户在其home目录内权限一般是不受限的，一旦出了home目录大多只有读和执行权限，没有修改权限



#### su

**用于账号切换的系统命令**

```c++
su - [用户名]
```

- -符号是可选的，表示是否在切换用户后加载环境变量、
- 用户名，表示要切换的用户，缺省为root
- 切换命令后，可使用exit或者ctrl+d退回上一个用户
- 使用普通用户切换到其他用户要输密码，由root用户切换到其他用户则不需要输密码





#### sudo

**给一条命令临时赋予root权限**

```c++
sudo 其他命令
```



但并不是所有的用户都有sudo权限，需要为普通用户配置sudo认证

- 切换到root用户，执行visudo命令，或者vim /etc/sudoers
- 在文件最后添加

```c++
creep ALL=(ALL) NOPASSWD:ALL
```

- 最后:wq! 退出





### 用户，用户组

linux系统可以

- 配置多个用户
- 配置多个用户组
- 用户可以加入多个用户组里



**linux关于权限管控级别有两个级别**

- 针对用户的权限控制
- 针对用户组的权限控制



**以下命令需要root权限**

####  创建用户组

```c++
groupadd 用户组名
```



#### 删除用户组

```c++
groupdel 用户组名
```



#### 创建用户

每个用户一定有一个主组，useradd的时候创建，名字默认和用户名相同

```c++
useradd [-g -d] 用户名
```

- -m 按照home路径创建目录（在/home路径下创建一个用户名文件夹）
- -g指定用户的主组的名字，不加-g会创建同名组并自动加入，如果已经存在同名组了必须-g
- -d制定用户的HOME目录，如果不加默认在/home/用户名



#### 删除用户

```c++
userdel [-r] 用户名
```

- -r,删除用户的HOME目录，不使用-r，HOME保留



#### id

```c++
id [用户名]
```

**查看·用户所属的组，如果不提供则查看自身**



#### 用户加入组

```c++
usermod -aG 用户组 用户名
```

将指定用户加入用户组里



#### getent passwd

**查看当前系统中有哪些用户**

共有7份信息

用户名：密码(x)：用户id：组id：描述信息：HOME目录：执行终端

#### getent group

**查看当前系统有哪些组**

共有3份信息

组名称：组认证(x):组id





#### 认知权限信息

ls -l以列表形式显示的信息

```c++
drwxr-x--- 3 creep creep 4096 Jan 21 15:05 creep
序号1         序号2 序号3
```

- 序号1：表示文件，文件夹的权限控制信息
- 序号2：表示文件，文件夹所属用户
- 序号3：表示文件文件夹所属用户组



##### 权限细节

**总共十个槽位**

- 第一个位置：-/d/l -表示是文件，d表示是文件夹，l表示是软链接
- 2~4位置，表示用户权限 r/- w/- x/- 分别表示有无 读，写，执行的权限
- 5~7位置，表示用户组权限 r/- w/- x/- 分别表示有无 读，写，执行的权限
- 8-10位置，表示其他用户权限  r/- w/- x/- 分别表示有无 读，写，执行的权限

e.g drwxr-xr-x 表示这是一个文件夹，用户有rwx，用户组有rx,其他用户有rx



- r针对文件可以查看文件内容，针对文件夹可以查看文件夹内容比如ls
- w针对文件表示可以修改此文件，针对文件夹表示可以在文件夹内创建，删除，改名等操作
- x针对文件表示可以将文件作为程序执行，针对文件夹表示可以更改工作目录到此文件夹如cd



#### chmod

修改权限信息（只有root或者文件/文件夹所属用户可以修改）

```c++
chmod [-R] 权限 文件/文件夹
chmod u=rwwx,g=rx,o=x hello.txt 将文件权限修改为:rwxr-x--x
```

- -R表示如果修改的是文件夹那么对这个文件夹里的文件都修改

##### 权限的数字序号

权限可以通过3位数字表示，第一个数字表示用户权限，第二个表示用户组权限，第三个表示其他用户权限

数字的细节，rwx表示三位2进制数 421 那么3表示-wx，5表示r-x

那么751表示 rwx r-x --x



#### chown

**修改文件/文件夹所属用户和用户组**

```c++
chown [-R] [用户][:][用户组] 文件/文件夹
```

- 选项 -R，同chmod，对文件夹内全部内容应用相同规则

```c++
chown root hello.txt 将hello.txt所属用户修改为root
chown :root hello.txt 将hello.txt所属用户组修改为root
chown root:creep hello.txt 用户修改为root 用户组修改为creep
chown -R root test 将test文件夹里所有内容和文件夹的用户修改为root
```

